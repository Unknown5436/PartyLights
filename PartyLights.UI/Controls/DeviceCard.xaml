<UserControl x:Class="PartyLights.UI.Controls.DeviceCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="80" d:DesignWidth="300">
    
    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Device Icon -->
            <Ellipse Grid.Column="0" 
                    Width="40" 
                    Height="40" 
                    Fill="{Binding Device.Type, Converter={StaticResource DeviceTypeToColorConverter}}"
                    Margin="0,0,12,0"/>
            
            <!-- Device Info -->
            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                <TextBlock Text="{Binding Device.Name}" 
                          FontSize="16" 
                          FontWeight="SemiBold" 
                          Foreground="{StaticResource TextBrush}"/>
                <TextBlock Text="{Binding Device.Type}" 
                          FontSize="12" 
                          Foreground="{StaticResource TextMutedBrush}"/>
                <TextBlock Text="{Binding Device.IpAddress}" 
                          FontSize="11" 
                          Foreground="{StaticResource TextMutedBrush}"/>
            </StackPanel>
            
            <!-- Status Indicator -->
            <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                <Ellipse Width="8" 
                        Height="8" 
                        Fill="{Binding Device.IsConnected, Converter={StaticResource BoolToStatusColorConverter}}"
                        Margin="0,0,4,0"/>
                <TextBlock Text="{Binding Device.IsConnected, Converter={StaticResource BoolToStatusTextConverter}}" 
                          FontSize="12" 
                          Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
